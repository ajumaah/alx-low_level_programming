#include "main.h"

/**
 * _strcat - a functions that concatenate two strings
 * @dest: first string
 * @src: second string
 *
 * Return: pointer to the resulting string dest
 */

char *_strcat(char *dest, char *src)
{
	int j, k;

	for (j = 0; dest[j] != '\0'; j++)
	{
	}
	for (k = 0; (dest[j + k] = *src++) != '\0'; k++)
	{
	}
	return (dest);
}
