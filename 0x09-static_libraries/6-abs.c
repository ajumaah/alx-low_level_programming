#include "main.h"

/**
 *_abs - computes the absolute value of an integer
 * @v: the number to print
 * Return: The absolute vale of an integer
 */
int _abs(int v)
{
	if (v >= 0)
	{
		return (v);
	}
	else
	{
		return (-v);
	}
}
