#include "main.h"
/**
 * main _isupper is the main function
 * Description: _isupper check for uppercase character.
 *@c the charaters to be cheked
 * Return Always 0 (Success)
 */
int _isupper(int c)
{
	char c;
	if (c == 'A')
		_putchar("%c: %d\n", c, _isupper(c));
	else
		_putchar("%c: %d\n", c, _isupper(c));
	return (0);
